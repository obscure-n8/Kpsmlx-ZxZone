version: "3.3"

services:
  app:
    build: .
    command: bash start.sh
    restart: unless-stopped
    ports:
      - "80:80"
      - "8080:8080"
    environment:
      - TZ=Asia/Dhaka
      - PORT=8080
    volumes:
      - ./downloads:/app/downloads
      - ./accounts:/app/accounts
